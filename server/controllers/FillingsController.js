const Filling = require('../models/Filling')
export const CreateFilling = (req, res, next) => {
  // req.body
  // validation, try-catch
  // const filling = await Filling.create(req.body)
  // return res.send(filling)
}
