redux -> initState
redux - action, for fetch products and set to state

useSelector ??? what is it?

отдельные папки со статикой прям рядом с компонентом
сделать шаблон кнопки и потом в него как параметры передавать и вызывать - должен быть по итогу один каркас
сделать отдельные компоненты для footer моделки кастом
mapping для сравнение категории филингс и каррентпайдж
нужно стилизовать инпут
поправить все в один стиль
write another useHttp for useHttpTimeouted
// classNames, styled-component
